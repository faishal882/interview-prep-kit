"use client";

// Phase 4 implements the full batch tab; this placeholder keeps the route alive.
export function BatchUpload() {
  return <p className="text-sm text-neutral-500">Batch upload lands in Phase 4.</p>;
}
